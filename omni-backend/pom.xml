<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.6</version>
        <relativePath/>
    </parent>

    <groupId>com.omni</groupId>
    <artifactId>omni-stack</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Omni-Stack</name>
    <description>Omni-Stack Microservices Platform</description>

    <modules>
        <module>omni-common-core</module>
        <module>omni-common</module>
        <module>omni-common-mybatis</module>
        <module>omni-common-redis</module>
        <module>omni-common-redis-reactive</module>
        <module>omni-common-operlog</module>
        <module>omni-common-job</module>
        <module>omni-common-mqlog</module>
        <module>omni-common-workflow</module>
        <module>omni-common-service</module>
        <module>omni-db-migrator</module>
        <module>omni-auth</module>
        <module>omni-base</module>
        <module>omni-workflow</module>
        <module>omni-crm</module>
        <module>omni-srm</module>
        <module>omni-procurement</module>
        <module>omni-asset</module>
        <module>omni-gateway</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <spring-cloud.version>2025.1.1</spring-cloud.version>
        <spring-cloud-alibaba.version>2025.1.0.0</spring-cloud-alibaba.version>
        <mybatis-plus.version>3.5.16</mybatis-plus.version>
        <xxl-job.version>3.3.1</xxl-job.version>
        <flowable.version>8.0.0</flowable.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Cloud Alibaba BOM -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Common Core Module -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Common Module -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Common MyBatis-Plus Starter Module -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common-mybatis</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Common Redis Starter Module -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common-redis</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Common Reactive Redis Starter Module -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common-redis-reactive</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Common OperLog Module -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common-operlog</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Common Job Module -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common-job</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Common MQ Log Module -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common-mqlog</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Common Workflow Module -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common-workflow</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Common Servlet Business Service Starter -->
            <dependency>
                <groupId>com.omni</groupId>
                <artifactId>omni-common-service</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- JDK 25 禁止 Mockito 继续依赖运行时自附加，测试进程显式加载其代理。 -->
                    <argLine>-javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
