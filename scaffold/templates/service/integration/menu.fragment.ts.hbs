// 由 @omni-stack/cli {{generatorVersion}} 生成；合并到 menuI18nMap 和 iconMap。
export const {{className}}MenuIntegration = {
  permissionCode: '{{serviceId}}:read',
  i18nKey: '{{serviceId}}.title',
  icon: 'Grid',
  view: '@/views/{{serviceId}}/index.vue',
}
