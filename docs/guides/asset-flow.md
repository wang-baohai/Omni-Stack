# 资产入账、领用、归还、调拨与处置完整业务流程

Asset 从采购合格收货事件建立资产卡片，并管理台账、分配、接收、归还、调拨和处置。金额字段通过 JSON 传输时使用十进制字符串，不能使用会丢精度的浮点数。

## 1. 收货建卡

Procurement 发布收货事件后，Asset 消费者只处理：

- `qualityStatus=PASS`。
- `assetManaged=true`。
- `assetQuantity` 为精确正整数。

事件 Inbox ID 与“收货行 + 单位序号”形成双重幂等。重复投递不会重复建卡。每张资产卡记录来源收货、采购金额、分类、状态和当前责任信息。

## 2. 资产台账

管理视图按 `owner_user_id/owner_unit_id` 应用数据权限；“我的资产”按 `current_user_id` 固定过滤。台账支持按编号、名称、状态、分类、部门和位置查询。

任何状态操作都通过专用命令执行，并校验当前状态和乐观锁版本。不能使用通用更新接口任意修改状态。

## 3. 分配、接收与归还

1. 管理员从在库资产发起分配，指定接收人和位置。
2. 接收人在“我的资产”确认接收。
3. 使用中资产可以申请或执行归还。
4. 归还完成后清理当前用户并恢复适当库存状态。

接收和归还始终校验 `current_user_id`，避免管理员数据范围被误用于替用户接收。

## 4. 调拨审批

调拨把资产从一个责任用户、部门或位置转移到另一个范围。提交后启动 Workflow。资产使用 `active_operation_type/active_operation_id` 占用，防止同一资产同时进入调拨和处置。

审批通过后原子更新责任信息并清除占用。取消或拒绝必须在同一事务恢复 `previous_asset_status` 并清除占用。

## 5. 处置审批

处置用于报废、出售、捐赠等终止资产生命周期的操作。提交前检查资产未被其他操作占用。审批通过后记录处置方式、日期、残值和原因；终态资产不能再次分配或调拨。

残值、采购金额和事件价格均使用十进制字符串。审批运行时仍在 `omni-workflow`，Asset 只通过协调器维护领域状态。

## 6. 维护与盘点边界

资产可进入维修中并在修复后归还原状态。当前 MVP 不把完整折旧、财务总账或复杂盘点算法伪装为已实现能力；项目扩展时应新增明确聚合和迁移，而不是复用处置字段。

## 7. 排查

- 收货后无资产：核对质量、资产化标志、精确数量和 Inbox 状态。
- 重复资产：核对事件 ID 与来源单位序号唯一约束。
- 调拨/处置无法提交：检查 active operation 占用。
- 审批结束状态未恢复：检查协调器补偿、Workflow 结果和事务边界。
- 用户看不到资产：区分管理 owner 范围与“我的资产” current user 范围。

完整约束见 [Asset 设计](../design/asset-design.md)。

